# ─────────────────────────────────────────────────────────────────────────────
#  requirements.txt — flat pin list for environments that do not use uv
#  Generated from pyproject.toml for convenience.
#
#  Install:   pip install -r requirements.txt
#  Dev tools: pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────

# MCP server SDK (includes FastMCP)
mcp>=1.8.0

# CrewAI framework
crewai>=0.80.0

# CrewAI tools — MCP adapter (MCPServerAdapter, StdioServerParameters)
crewai-tools[mcp]>=0.14.0

# Environment variable loading
python-dotenv>=1.0.1

# Data handling
pandas>=2.2.0

# Input validation
pydantic>=2.7.0

# HTTP client
httpx>=0.27.0
